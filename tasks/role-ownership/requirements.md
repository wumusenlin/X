# 角色主责协作规则

## 目标

在独立子项目保持各自 Git 历史的前提下，明确后端、前端、产品/小控和绩效平台的默认主责目录，并为跨项目需求提供单任务归口方式。

## 范围

- 范围内：根仓角色规则、任务模板和入口文档。
- 范围外：子项目业务代码、Git 分支策略和自动权限控制。

## 验收

1. 每个项目有明确默认主责角色和目录。
2. 跨项目任务有唯一主责，并记录跨域修改的接管范围。
3. Web 宿主与小控的嵌入边界明确。
4. 共享接口继续以 `contracts/` 为唯一共享入口。
