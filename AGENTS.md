# Agent 工作区规则

本仓库是多个独立业务项目的协作工作区。根目录只负责路由、跨项目边界和协作记录；进入子项目后必须遵守其自身的 `AGENTS.md`。

## 项目映射

- `webapp` 是 Web 端项目。
- `mobile` 是移动端项目。
- `xiaokong` 是前端口的智能对话项目，独立项目，通过 iframe 内嵌到 Web 端。
- `nky` 是后端项目。
- 绩效平台当前位于工作区外的 `/Users/zhangsenlin/nky/code/2025-02-11/bureauweb`；尚未映射到 `bureau/`。
- 根目录下的每个子项目都应保留自己的 `.git` 目录和独立提交历史。

## 角色主责

- 后端主责 `nky/**`。
- 前端主责 `webapp/**` 和 `mobile/**`。
- 产品/小控主责 `xiaokong/**`，并维护跨项目任务的需求与验收口径。
- 绩效平台主责外部 `bureauweb` 仓库；纳入 `bureau/` 前不得假设该目录存在。
- 协作规范、模板和编排机制以 `agent-sop/` 为准；跨域修改必须在任务档案中明确唯一主责、协作角色、接管范围和验证责任。

## 工作规则

- 将 `X` 作为多个独立项目的统一入口，不要假设子项目属于 X 根仓库的 Git 历史。
- 当前任务、共享需求、接口约定、决策、验证和交接记录统一落在 `agent-workspace/<主责项目>/<任务ID>/`。跨项目任务只保留一份主档案，`SPEC.md` 必须列明全部受影响项目。
- `SPEC.md` 记录目标、范围、禁区、主责、跨域接管、接口约定和验收标准；接口变更冻结后不得擅自改写，必须重新确认。
- `PLAYBOOK.md` 是断点恢复的唯一状态文件；`TASK_LOG.md` 只追加事件和证据；`HANDOVER.md` 记录交接、遗留和发布顺序。模板及字段规则先读 `agent-sop/` 与 `agent-workspace/AGENTS.md`。
- 接口字段快照、请求/响应样例等契约证据随任务档案保存；落盘前必须脱敏手机号、身份证、金额、真实姓名等业务数据。
- 需要定位近期修改或续接任务时，先读对应任务的 `PLAYBOOK.md` 和 `SPEC.md`，再查 `product-docs/` 的能力与发布记录，最后以子项目 Git SHA 和 diff 为准。
- `product-docs/` 只记录已验证的产品能力、业务规则、用户旅程、决策和发布变化；不复制实现细节、枚举值或接口字段。
- 禁止使用 npm，必须使用 pnpm。

## 变更纪律

- 修改任何子项目代码前，必须先阅读并遵守该子项目内的 `AGENTS.md`，随后查看该项目的 README、包配置文件和测试命令。
- 除非用户明确要求，否则每个子项目分别提交。
- 收到“提交代码”或“推送代码”但未指定仓库时，必须先确认目标是 X 根仓库、任务档案仓、产品文档仓，还是具体子项目；不得将独立子项目目录作为 X 根仓库内容暂存或提交。
- 完成任一子项目的 `git commit` 或 `git push` 后，必须在对应任务的 `TASK_LOG.md` 追加项目、分支、完整 SHA、验证结果和时间；在 `HANDOVER.md` 或 `PLAYBOOK.md` 更新发布顺序与遗留事项。任务档案仓和产品文档仓均独立提交，未经用户授权不得顺带提交或推送。
- 每个项目的改动应尽量小，便于审查；除非用户明确要求，否则不要重写子项目历史。

## 默认跨项目流程

1. 先查 `product-docs/INDEX.md` 和相关能力文档，确认已有产品规则。
2. 确认或创建 `agent-workspace/<主责项目>/<任务ID>/` 的任务档案，并读取 `agent-workspace/AGENTS.md`。
3. 在 `SPEC.md` 冻结需求、主责、受影响项目、接口约定、边界和验收标准；在 `PLAYBOOK.md` 记录当前步骤和决策理由。
4. 按受影响项目实现存储、接口或界面。
5. 在每个被修改的项目中运行针对性验证，并将结果追加到 `TASK_LOG.md`。
6. 收口时更新 `HANDOVER.md`；若产品对用户的承诺发生变化，再更新 `product-docs/` 的能力文档和发布记录。
