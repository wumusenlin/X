# Agent 工作区规则

`X` 是多仓协作入口，不是单一项目。根目录只负责路由、跨项目边界和协作记录；实现规则、构建命令和代码规范以目标仓库自己的 `AGENTS.md` 为准。

## 目录与边界

| 目录 | 定位 | 处理规则 |
| --- | --- | --- |
| `nky/` | Java 后端 | 独立 Git 仓；后端主责。 |
| `webapp/` | Web 前端 monorepo | 独立 Git 仓；前端主责。 |
| `mobile/` | 移动端前端 | 独立 Git 仓；前端主责。 |
| `xiaokong/` | 智能问答前端 | 独立 Git 仓；产品/小控主责，通过 iframe 嵌入 Web 端。 |
| `framework/` | `nky` 依赖的基础框架与代码生成 | 独立 Git 仓；先评估对 `nky` 的影响。 |
| `agent-sop/` | 协作机制、模板和编排规则 | 独立 Git 仓；根入口规则的可同步源在 `agent-sop/root/`。 |
| `agent-workspace/` | 任务运行档案 | 独立 Git 仓；每轮任务按 `<主责项目>/<任务ID>/` 保存。 |
| `product-docs/` | 已验证的产品能力总账 | 独立 Git 仓；只记录产品事实，不记录实现细节。 |

- `.m2/`、`.pnpm-store/`、`.local-tools/`、`out/` 等是本地工具或构建产物，不是项目仓库；不得将其当作业务改动范围或提交内容。
- 绩效平台位于工作区外的 `/Users/zhangsenlin/nky/code/2025-02-11/bureauweb`；未纳入本工作区前不得假设存在 `bureau/`。
- 未列目录或外部项目（如 VideoAll）仅在用户明确点名后处理，并优先遵守其自己的 `AGENTS.md` 和发布规则。
- 不要把任何子仓当作 X 根仓库历史的一部分，也不要在根仓暂存子仓文件。

## 任务档案与产品文档

- 纯问答、代码定位、现状核对和仅修改根治理文档不创建任务档案。
- 对 `nky`、`webapp`、`mobile`、`xiaokong` 的功能、缺陷或接口改动，先查 `product-docs/INDEX.md` 及相关能力文档，再确认或创建 `agent-workspace/<主责项目>/<任务ID>/`。
- 跨项目任务只保留一份主档案；`SPEC.md` 必须写明唯一主责、全部受影响项目、跨域接管、接口约定、边界和验收标准。
- `PLAYBOOK.md` 是断点恢复状态；`TASK_LOG.md` 只追加事件与证据；`HANDOVER.md` 记录交接、遗留和发布顺序。模板和字段规则以 `agent-sop/` 与 `agent-workspace/AGENTS.md` 为准。
- 请求/响应样例和接口快照随任务档案保存，落盘前必须脱敏手机号、身份证、金额、真实姓名等业务数据。
- `product-docs/` 只在产品对用户的承诺发生变化且已验证后更新；不复制代码路径、构建命令、枚举值、接口字段或 SQL。

## 变更纪律

- 修改子项目代码前，必须先阅读该子项目的 `AGENTS.md`，再查看 README、包配置或构建文件及其测试命令。
- Node 项目使用 `pnpm`；不得用 `npm` 替代项目既有包管理方式。Java 和其他项目按各自仓库规则执行。
- 改动保持在最窄的真实问题边界；未经用户明确要求，不删除文件、不迁移目录、不重写历史或扩大为架构重构。
- 每个独立仓库分别审查、验证、提交和推送。用户只说“提交”或“推送”而未指定目标时，先确认是 X 根仓、任务档案仓、产品文档仓还是具体子仓。
- 不主动提交或推送；用户明确授权后，授权仅覆盖本次指定仓库和范围。完成子仓提交或推送后，在对应 `TASK_LOG.md` 追加项目、分支、完整 SHA、验证结果和时间，并更新 `PLAYBOOK.md` 或 `HANDOVER.md` 的发布顺序与遗留。

## 默认跨项目流程

1. 查 `product-docs/INDEX.md` 和相关能力文档，确认现有产品规则。
2. 确认或创建任务档案，冻结 `SPEC.md` 中的范围、主责、接口和验收标准。
3. 分别在受影响子仓按各自规则实现，并运行针对性验证。
4. 将验证证据追加到 `TASK_LOG.md`，更新 `PLAYBOOK.md` 和 `HANDOVER.md`。
5. 产品承诺变化时，更新 `product-docs/` 的能力文档和发布记录。
